# Mini ERP + CRM Operations Portal

This repository has been initialized as a full-stack application scaffold.

## Current scope

- Frontend: React + TypeScript + Vite
- Backend: Node.js + TypeScript + Express
- API: REST health endpoint at `/api/health`

This repository intentionally does not yet implement authentication, database models, UI pages, business logic, or APIs beyond the minimal health endpoint.
